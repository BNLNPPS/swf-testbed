name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  integration-test:
    uses: ./.github/workflows/integration-test.yml
    with:
      swf_testbed_ref: ${{ github.sha }}
      swf_monitor_ref: main
